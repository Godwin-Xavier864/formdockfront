Add your HTML templates in `public/templates/files/`.

Rules:
- Use `.html` extension.
- File name becomes template name automatically.
- On `npm run dev` and `npm run build`, `public/templates/index.json` is generated.

Example:
- `public/templates/files/lead-gen.html`
- `public/templates/files/support-form.html`
